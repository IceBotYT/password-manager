<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, inital-scale=1">
  <meta charset="utf-8">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
  <title>Register</title>
  <style>
    html {
      padding: 0px;
      margin: 0px;
      background-color: white;
      color: black;
      font-size: 20px;
      z-index: 1;
    }
    #login-form {
      z-index: 2;
    }
    img {
            height: 100%;
            width: 100%;
            object-fit: contain;
          }

          .crop {
            max-height: 0px;
            overflow: hidden;
          }
    /* The snackbar - position it at the bottom and in the middle of the screen */
    #snackbar {
            visibility: hidden; /* Hidden by default. Visible on click */
            min-width: 250px; /* Set a default minimum width */
            margin-left: -125px; /* Divide value of min-width by 2 */
            background-color: #333; /* Black background color */
            color: #fff; /* White text color */
            text-align: center; /* Centered text */
            border-radius: 2px; /* Rounded borders */
            padding: 16px; /* Padding */
            position: fixed; /* Sit on top of the screen */
            z-index: 1; /* Add a z-index if needed */
            left: 50%; /* Center the snackbar */
            bottom: 30px; /* 30px from the bottom */
          }
  
          /* Show the snackbar when clicking on a button (class added with JavaScript) */
          #snackbar.show {
            visibility: visible; /* Show the snackbar */
            /* Add animation: Take 0.5 seconds to fade in and out the snackbar.
            However, delay the fade out process for 2.5 seconds */
            -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
            animation: fadein 0.5s, fadeout 0.5s 2.5s;
          }
          /* Animations to fade the snackbar in and out */
          @-webkit-keyframes fadein {
            from {bottom: 0; opacity: 0;}
            to {bottom: 30px; opacity: 1;}
          }
  
          @keyframes fadein {
            from {bottom: 0; opacity: 0;}
            to {bottom: 30px; opacity: 1;}
          }
  
          @-webkit-keyframes fadeout {
            from {bottom: 30px; opacity: 1;}
            to {bottom: 0; opacity: 0;}
          }
  
          @keyframes fadeout {
            from {bottom: 30px; opacity: 1;}
            to {bottom: 0; opacity: 0;}
          }
          #register-form {
            width: 450px;
            height: 250px;
            position: absolute;
            top: 30%;
            left: 35%;
            margin-left: -30px;
            margin-top: -10px;
            background-color: black;
            border-radius: 10px;
            size: 50%;
            color: white;
            z-index: 5;
          }
          .background {
            position: absolute;
            display: block;
            top: 0;
            left: 0;
            z-index: 0;
          }
          #button-group {
            position: absolute;
            top: 75%;
            left: 30%
          }
          .landscape {
            background-position: center center;
            background-color: white;
            z-index: 100;
            width: 0px;
            height: 0px;
            display: none;
          }
  </style>
  <script src="https://kit.fontawesome.com/d2dd9afdb0.js" crossorigin="anonymous"></script>
</head>
<body>
<div class="landscape container">
  <i class="fas fa-exclamation-triangle fa-5x" style="position: fixed; left: 35%; top: 25%"></i>
  <p class="text-center" style="position: fixed; top:50%">For a better experience, please rotate your device.</p>
</div>
<div id="good">
<div id="register-form" class="container">
<h1 class="text-center">Register</h1>
<form action="/register" method="POST" style="width:fit-content;">
  <div class="form-group text-center row">
    <label for="name" class="text-center col-sm">Name</label>
    <input type="text" id="name" name="name" class="col-sm" required>
  </div>
  <div class="form-group text-center row">
    <label for="email" class="text-center col-sm">Email</label>
    <input type="email" id="email" name="email" class="col-sm" required>
  </div>
  <div class="form-group text-center row">
    <label for="password" class="text-center col-sm">Password</label>
    <input type="password" id="password" name="password" class="col-sm" required>
  </div>
  <div class="text-center btn-group" id="button-group">
  <button type="submit" class="btn btn-primary mr-1">Register</button>
  <a href="/login" class="btn btn-info">Login</a>
  </div>
</form>
</div>
<script>
  window.onload = function() {
    if(window.innerHeight > window.innerWidth){
      var alert = document.getElementsByClassName("landscape")
      var main = document.getElementById('good')
      alert[0].style.width = window.innerWidth + "px"
      alert[0].style.height = window.innerHeight + "px"
      main.style.display = "none";
      alert[0].style.display = "block";
    }
    Particles.init({
      selector: '.background',
      // connectParticles: true,
      color: "#4DB3B3",
      maxParticles: 250
    })
    if (window.innerWidth < 1500) {
      var crop = document.getElementsByClassName("crop")
      var bg = document.getElementsByClassName("background")
      var div = document.getElementById('register-form')
      bg[0].style.display = "none";
      crop[0].style.display = "none";
      div.style.left = "20%"
      div.style.width = "500px"
      div.style.height = "400px"
    }
    var crop = document.getElementsByClassName("crop")
    crop[0].style.maxHeight = window.innerHeight + "px"
    crop[0].style.maxWidth = window.innerWidth + "px"
  }
  window.onresize = function() {
    setTimeout(function() { // iOS sucks
      if(window.innerHeight > window.innerWidth){
      var alert = document.getElementsByClassName("landscape")
      var main = document.getElementById('good')
      alert[0].style.width = window.innerWidth + "px"
      alert[0].style.height = window.innerHeight + "px"
      main.style.display = "none";
      alert[0].style.display = "block";
    } else {
      var alert = document.getElementsByClassName('landscape')
      var good = document.getElementById('good')
      alert[0].style.display = "none";
      good.style.display = "block";
    }
    if (window.innerWidth < 1500) {
      var crop = document.getElementsByClassName("crop")
      var bg = document.getElementsByClassName("background")
      var div = document.getElementById("register-form")
      bg[0].style.display = "none";
      crop[0].style.display = "none";
      div.style.left = "20%"
    }
    var crop = document.getElementsByClassName("crop")
    crop[0].style.maxHeight = window.innerHeight + "px"
    crop[0].style.maxWidth = window.innerWidth + "px"
    }, 100)
  }
</script>
<div class="crop">
  <img src="/lights">
  </div>
<canvas class="background"></canvas>
<script src="https://cdnjs.cloudflare.com/ajax/libs/particlesjs/2.2.2/particles.min.js"></script>
</div>
</body>
</html>